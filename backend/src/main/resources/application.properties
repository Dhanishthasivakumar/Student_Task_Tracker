spring.application.name=studenttracker

# Server configuration
server.port=8081

# Database Configuration (MySQL)
# Update 'student_tracker_db', 'root', and 'password' with your local MySQL credentials
spring.datasource.url=jdbc:mysql://localhost:3306/student_tracker_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Dh_2812006

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# JWT Secret Key (In a real app, externalize this!)
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=86400000

# Enable data.sql seed script to run on startup
# Set to 'never' since we use DataInitializer (CommandLineRunner) instead
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=true
